<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.skhu.traveler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Traveler">
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".JoinActivity" />
        <activity android:name=".SearchListActivity2"></activity>
        <activity android:name=".MyScheduleActivity2" />
        <activity android:name=".CheckReservationActivity" />
        <activity android:name="net.skhu.notice.NoticeActivity"></activity>
        <activity android:name="net.skhu.notice.NoticeBoardActivity"></activity>
        <activity android:name="net.skhu.notice.TemporaryNoticeActivity"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDAFVdnZUaDPuIm2c4YDbN5Mn_fniU_gt8" />

        <activity android:name=".MapActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>